meta {
  name: User Info
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/usebruno
  body: none
  auth: inherit
}

tests {
  test("status must be 200", function() {
    expect(res.status).to.eql(201);
  });
}

docs {
  This is the doc for User Info
}

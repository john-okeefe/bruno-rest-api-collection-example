meta {
  name: Login User
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "user": "Test User",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Docs for Login User.
}
